mod ChessBoardLib;

use ChessBoardLib as cbl;

fn main() {
    println!("Hello, world!");
    unsafe
    {
        // let mut chessboard = cbl::ChessBoard::new(6, 8);
        // let mut target_str: cbl::std_basic_string<i8>;
        // let mut drawer = cbl::StringDrawer::new(&chessboard, &mut target_str);
        // TODO: make user interface for using the lib in the lib itself
    }
}
